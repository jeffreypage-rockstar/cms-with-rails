<%= form_for(@user_follower) do |f| %>
  <% if @user_follower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_follower.errors.count, "error") %> prohibited this user_follower from being saved:</h2>

      <ul>
      <% @user_follower.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :follower %><br>
    <%= f.number_field :follower %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
