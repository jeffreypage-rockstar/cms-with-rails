== BizGo

Here is how to get this app up and running.

Things you need or may want:

* Ruby 2.1.3 (Install using `rvm`)

* Postgres 9.3

* Bundler

* Foreman

Once all requirements are set up, run `cat .env-example > .env` the `rake db:create` 
then `rake db:migrate` then `foreman start`
